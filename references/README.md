# References

This directory is intentionally lightweight in the public repository.

Local users may generate `official_*` reference files from their own licensed
Sentaurus installation using `scripts/build_official_indexes.py`. Those files
are ignored by git and should remain local unless the user has independently
verified redistribution rights.

